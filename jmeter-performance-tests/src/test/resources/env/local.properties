# Optional local notes for the Maven/JMeter runtime.
# Maven passes runtime values with -D properties; keep real credentials out of this file.

base.url=http://localhost:8080
threads=10
ramp.up.seconds=30
duration.seconds=60
max.response.ms=1500
